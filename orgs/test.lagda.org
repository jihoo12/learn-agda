#+title: test
* it is lagda.org file
#+begin_src agda2
{-# OPTIONS --cubical #-}
module test where

open import Agda.Primitive.Cubical
open import Agda.Builtin.Cubical.Path

data Torus : Set where
    point : Torus
    line1 : point ≡ point
    line2 : point ≡ point
    square : PathP (λ i → line1 i ≡ line1 i) line2 line1
#+end_src
